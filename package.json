{
  "name": "lana",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cf:types": "wrangler types",
    "dev": "npm run cf:types && next dev",
    "build": "npm run cf:types && next build",
    "start": "next start",
    "lint": "eslint",
    "cf:build": "npm run cf:types && opennextjs-cloudflare build",
    "cf:preview": "npm run cf:types && opennextjs-cloudflare build && wrangler dev",
    "cf:deploy": "npm run cf:types && opennextjs-cloudflare build && opennextjs-cloudflare deploy",
    "db:migrate:local": "bash scripts/migrate.sh --local",
    "db:migrate:remote": "bash scripts/migrate.sh --remote"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "iron-session": "^8.0.4",
    "next": "16.2.10",
    "plaid": "^43.0.0",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-plaid-link": "^4.1.1",
    "recharts": "^3.9.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.20.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.107.0"
  }
}
